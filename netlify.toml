# GradOps — Next.js 14 on Netlify
# Netlify auto-detects Next.js and uses the OpenNext adapter (no custom publish dir).

[build]
  command = "npx prisma generate && npm run build"

[build.environment]
  NODE_VERSION = "18"
